[Unit]
Description=MyMolt Core
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/mymolt
ExecStart=/opt/mymolt/mymolt start
Restart=always
RestartSec=5
Environment=RUST_LOG=info
# Environment=MYMOLT_CONFIG=/opt/mymolt/config.toml

[Install]
WantedBy=multi-user.target
